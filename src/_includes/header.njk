<nav class="bg-gray-800 shadow-md lg:sticky top-0 z-10 text-white text-lg">
    <div class="mx-auto max-w-7xl px-2 sm:px-4 lg:px-8">
        <div class="relative flex h-16 items-center justify-between">
            <div class="flex items-center px-2 lg:px-0">
                <div class="flex-shrink-0">
                    <img class="h-8 w-auto" src="assets/img/favicon-32x32.png" alt="Letter J Logo" />
                </div>
                <div class="hidden lg:ml-6 lg:block">
                    <div class="flex space-x-4">
                        <a href="#" class="rounded-md bg-gray-900 px-3 py-2 text-white hover:bg-purple-600">Home</a>
                        {% for link in links.navigation %}
                            <a class="rounded-md px-3 py-2 text-gray-300 hover:bg-purple-500"
                                href={{ link.href }}
                                {% if 'http' in link.href %} target="_blank" {% endif %} >
                                {{ link.text }}
                            </a>
                        {% endfor %}
                    </div>
                </div>
            </div>
            <div class="flex flex-1 justify-center px-2 lg:ml-6 lg:justify-end">
                <div class="hidden lg:ml-6 lg:block">
                    <div class="flex space-x-4">
                        {% for link in links.social %}
                            <a class="rounded-md px-3 py-2 text-gray-300 hover:bg-purple-500"
                                href={{ link.href }}
                                {% if 'http' in link.href %} target="_blank" {% endif %} >
                                {{ link.text }}
                            </a>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="lg:hidden" id="mobile-menu">
        <div class="space-y-1 px-2 pb-3 pt-2">
            <a href="/" class="block rounded-md bg-gray-900 px-3 py-2 text-base text-white">Home</a>
            {% for link in links.navigation %}
                <a class="block rounded-md px-3 py-2 text-base text-gray-300 hover:bg-gray-700 hover:text-white"
                    href={{ link.href }}
                    {% if 'http' in link.href %} target="_blank" {% endif %} >
                    {{ link.text }}
                </a>
            {% endfor %}
        </div>
        <div class="border-t border-gray-700 py-3">
            <div class="mt-3 space-y-1 px-2">
                {% for link in links.social %}
                    <a class="block rounded-md px-3 py-2 text-base text-gray-400 hover:bg-gray-700 hover:text-white"
                        href={{ link.href }}
                        {% if 'http' in link.href %} target="_blank" {% endif %} >
                        {{ link.text }}
                    </a>
                {% endfor %}
            </div>
        </div>
    </div>
</nav>
